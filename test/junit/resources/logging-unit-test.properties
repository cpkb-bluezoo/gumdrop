# Default logging for ant test (forked JUnit JVMs). Gumdrop code paths that
# deliberately handle client/protocol errors should not flood CI with WARNING
# lines; tests that assert on logging attach their own handlers explicitly.
handlers=java.util.logging.ConsoleHandler
java.util.logging.ConsoleHandler.level=ALL
.level=INFO
org.bluezoo.gumdrop.level=SEVERE
