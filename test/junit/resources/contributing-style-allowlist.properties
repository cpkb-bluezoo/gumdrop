# Known style debt permitted until remediated. Paths use root:relPath
# (main | junit | integration | examples). Empty lists mean no exceptions.

allowlist.lambda=

allowlist.methodref=

allowlist.virtualthread=main:servlet/Container.java

allowlist.scheduled=

allowlist.functionalinterface=

allowlist.future=
